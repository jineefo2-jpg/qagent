# 样本外运行台账（D7）

> 样本外区间 2020-01-01 至今**只跑一次**。每次运行由 `run_backtest` **自动追加**一行（不靠人工）。
> 改任何参数后重跑，等于把样本外污染成样本内 —— 必须在此标注。
> `param_hash` 锁参数，`data_snapshot_id` 锁数据；两者任一不同，结果不可与历史行直接比较。

**状态：P2 交付（2026-08-24）为止，本期【未执行任何样本外运行】—— 下表为空即是证据。**

这是有意的，不是待办事项：D7 只允许一次，而此刻 market 库还没回补，
因子也还没落库。在数据齐备、五闸（`ashare/backtest/guards.py`）能真正跑起来之前把它烧掉，
是这一次机会最坏的用法 —— 之后任何「再跑一次看看」都已经是拿样本外调参。
样本外运行属 P3（计划自查的已知缺口 U6）。

表里出现第一行时，上面这段话即作废；届时请把它改成对那一行的说明，不要删掉整段。
`Sharpe(IS)` 列由**闸 1** 成对写入（`store.append_oos_run(result, sharpe_is=...)`）——
引擎自动追加时只看得见一条曲线，那一格会是「—」，此时 `SR_oos ≥ 0.6·SR_is` 无从当场核对。

| 运行时间 (UTC) | strategy_version | param_hash | data_snapshot_id | engine_version | 区间 | Sharpe(IS) | Sharpe(OOS) | 五闸 | 备注 |
|---|---|---|---|---|---|---|---|---|---|
