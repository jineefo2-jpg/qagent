# 样本外运行台账（D7）

> 样本外区间 2020-01-01 至今**只跑一次**。每次运行由 `run_backtest` **自动追加**一行（不靠人工）。
> 改任何参数后重跑，等于把样本外污染成样本内 —— 必须在此标注。
> `param_hash` 锁参数，`data_snapshot_id` 锁数据；两者任一不同，结果不可与历史行直接比较。

**状态：2026-08-28 04:05 执行了唯一一次样本外运行 —— 下表两行即是它。机会已用尽。**

（此前的状态说明按其自身规定作废，改写如下 —— 原文承诺「表里出现第一行时把这段
改成对那一行的说明，不要删掉整段」。）

**这两行是同一次仪式的两条臂，不是两次机会。** 预注册清单（`docs/plans/2026-08-26-p3-
strategy-and-signals.md` §Task 9）在**看到任何样本外数字之前**冻结了全部判据，脚本
`scripts/oos_ceremony_2026-08-27.py burn` 无条件连跑两臂（不许看完一臂再决定另一臂跑不跑）：

| | 配置 | 样本内 Sharpe | 样本外 Sharpe | 门槛 0.6×IS | 闸 1 |
|---|---|---|---|---|---|
| A | 恒定仓位 `macro_timing=False, cap=0.8` | 0.4891 | **0.4156** | 0.2934 | 过（比值 0.850） |
| B | 宏观择时 `macro_timing=True, [0.2,1.0]` | 0.4835 | 0.3929 | 0.2901 | 过（比值 0.813） |

**判定（按预注册判据机械得出）**
1. **宏观择时层关停**：判据要求 B 的 Sharpe **且** Calmar 双超 A；Sharpe 0.3929 < 0.4156
   当场不成立，「且」条件失败，Calmar 无须再算（也因此没有算 —— 事后补算一个不影响
   结论的数字，是给自己留腾挪空间）。按规格 §6.1，π ≡ 0.8 常数化，宏观层降级为纯展示。
2. **闸 1 施加于胜出臂 A：过**。样本外保住样本内 Sharpe 的 85%，门槛是 60%。
3. **生产配置 = A 臂**：`macro_timing=False, position_cap=0.8`，`param_hash` 见下表。

**前置**（同一快照 `c2cd172976e1f3fe`，闸 2/4/5 在缺陷修复后复跑逐字一致）：
闸 2 拼接样本外 Sharpe 0.5940 ✅ / 闸 3 置换 p=0.0050 ✅ / 闸 4 成本×2 IR 0.2758 ✅ /
闸 5 PeakRatio 1.0128 ✅。详见 `out/oos_ceremony/gates_prelaunch.json`。

**下表两行的两处「—」与「未跑」是引擎自动追加的局限，不是事实**：`run_backtest` 一次
只看得见一条曲线，既不知道配对的样本内 Sharpe，也不知道闸跑没跑。真值在上表。
（`store.append_oos_run(result, sharpe_is=...)` 这个入口是 2026-08-24 为此加的，但
`gate1_out_of_sample` 从未调用它 —— 已知缺陷，修它属下一次改动，不在本仪式内动手。）

| 运行时间 (UTC) | strategy_version | param_hash | data_snapshot_id | engine_version | 区间 | Sharpe(IS) | Sharpe(OOS) | 五闸 | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| 2026-08-27 20:05 | reversal_20+momentum_120_20+volatility_60+turnov | 601ade2d15ac96f9 | c2cd172976e1f3fe | p2-engine-1 | 2020-01-01~2026-08-21 | — | 0.416 | 未跑 | 未跑：闸1 样本外/闸2 walk-forward/闸3 shuffle/闸4 成本敏感/闸5 参数高原 |
| 2026-08-27 20:07 | reversal_20+momentum_120_20+volatility_60+turnov | 9cc2707b6fa1cf04 | c2cd172976e1f3fe | p2-engine-1 | 2020-01-01~2026-08-21 | — | 0.393 | 未跑 | 未跑：闸1 样本外/闸2 walk-forward/闸3 shuffle/闸4 成本敏感/闸5 参数高原 |
