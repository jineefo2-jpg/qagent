[project]
name = "quant-agent-demo"
version = "0.0.0"
requires-python = ">=3.9"
description = "Quant trading AI agent demo (local dev / IDE metadata only)"

[tool.pyright]
venvPath = "."
venv = ".venv"
